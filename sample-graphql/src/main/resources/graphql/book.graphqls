#查询入口
type BookQuery {
    findAllBooks: [Book]!
    countBooks: Int!
}

type Book {
    id: String!
    title: String!
    isbn: String!
    pageCount: Int
    author: Author
}

input BookInput {
    title: String!
    isbn: String!
    pageCount: Int
    authorId: Int
}

